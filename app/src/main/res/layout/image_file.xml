<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/grid_image_height"
    android:layout_margin="@dimen/margin_grid"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="horizontal">
    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:cardElevation="@dimen/grid_elevation_cardView"
        android:padding="@dimen/grid_padding_cardView"
        android:layout_margin="@dimen/grid_margin_cardView"
        >
    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:src="@mipmap/ic_launcher"
        android:id="@+id/imageView"
        android:contentDescription="@string/grid_image_view" />
    </androidx.cardview.widget.CardView>

</LinearLayout>